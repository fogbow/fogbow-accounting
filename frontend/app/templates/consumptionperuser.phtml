<div class="row">
	<div class="col-md-12">
        <p><a href="#/home">Home</a> &gt; <strong>Consumption on {{memberId}}</strong></p>
		<table class="table table-condensed table-striped">
    		<thead>
    			<tr>
    				<th>Federation user</th>
    				<th>Consumed</th>
    			</tr>
    		</thead>
    		<tbody>
    			<tr ng-repeat="use in memberUsers">
    				<td>{{use.user}}</td>
    				<td>{{use.usage}}</td>
    			</tr>
    		</tbody>
    	</table>
	</div>
</div>